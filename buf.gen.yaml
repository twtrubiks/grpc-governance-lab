# 代碼生成設定:生成的 *.pb.go 與 .proto 同目錄(paths=source_relative),
# 並提交進版控,讓 clone 下來不裝 protoc 也能編譯(api/proto/README.md)。
version: v2
plugins:
  - local: protoc-gen-go
    out: api/proto
    opt: paths=source_relative
  - local: protoc-gen-go-grpc
    out: api/proto
    opt: paths=source_relative
